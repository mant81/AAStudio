# Agent Rules

- 프로젝트 최초 로딩 시 `AGENTS.md`를 가장 먼저 참조한다.
- 최초 현황 분석과 개발 계획은 하네스 기반으로 시작한다.
- 기존 하네스 결과서는 `docs/new/working_history/`의 최신 문서부터 읽고, `다음 단계 참조` 블록을 후속 작업의 진입점으로 사용한다.
- 반복 분석은 Graphify로 코드·결정·의존 관계를 조회해 전체 파일 재독해와 토큰 사용을 줄인다.
- 인코딩은 무조건 UTF-8로 한다.
- PowerShell은 사용하지 않는다.
- Git Bash를 우선 사용한다.
- `cmd`는 제일 후 순위로 사용한다.
- 수정 전에 원본 파일을 `history`에 복사한 후 작업한다.
- 파일을 열기 전에 원본 파일 바이트 그대로를 먼저 `history`에 복사한다. 내용을 메모리에 올린 뒤 저장한 백업은 원본 백업으로 간주하지 않는다.
- 반복 수정 중엔 백업을 생략한다.
- 백업 파일명은 원본 확장자 뒤에 `.yyyymmddhhmmss` 형식의 타임스탬프를 붙인다.
- 인코딩이 불명확한 파일은 원본 인코딩을 먼저 확인하고, UTF-8로 안전하게 변환 가능한 경우에만 수정한다.
- 인코딩이 깨진 상태로 읽힌 내용은 신뢰하지 않고, 그 상태로 파일에 다시 저장하지 않는다.
- `git checkout`, `git restore`, `git reset` 등으로 원본 기준 복구를 바로 수행하지 않는다. 최근 작업본은 별도 파일이나 `history`에 먼저 보존한 뒤 복구 여부를 판단한다.
- 인코딩 문제나 복구 작업이 필요한 경우에는 파일 전체를 한 번에 덮어쓰지 말고 작은 단위로 나누어 수정해 작업 리셋을 방지한다.
- 사용자 응답의 한글 설명은 20자 이내로 쓴다.
- 모바일은 우선 제외한다.

## AAStudio 프로젝트 구조 규칙

- AAStudio의 최상위 작업 단위는 `프로젝트`이다.
- 프로젝트 하위에는 `다이어그램`, `DB 모델링`, `API 정의서` 기능 모듈을 둔다.
- 각 기능 모듈은 여러 개의 하위 탭을 가질 수 있다.
- 다이어그램 모듈의 탭 하나는 하나의 독립된 다이어그램 작업 문서이자 그룹으로 취급한다.
- 다이어그램의 노드, 프레임, 연결선, 화면 위치, 선택 상태, 실행취소·다시실행 이력은 탭별로 독립 관리한다.
- 다이어그램 화면에서 `새로 만들기`는 기존 캔버스를 초기화하지 않고 새로운 다이어그램 하위 탭을 추가한다.
- 기존 탭 초기화는 사용자가 명시적으로 선택하는 별도 기능으로만 제공한다.
- 라이브러리 템플릿과 새 요소는 현재 활성화된 하위 탭에 추가한다.
- 프로젝트 저장·불러오기는 기능 모듈과 모든 하위 탭의 계층 및 상태를 함께 보존해야 한다.
- DB 모델링과 API 정의서도 다이어그램과 동일한 하위 탭 모델을 공유할 수 있도록 설계한다.
- 다크 모드는 제공하지 않는다.
