<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aastudio.mapper.UserMapper">
    <select id="selectUserByUsername" parameterType="string" resultType="com.aastudio.domain.auth.AuthVO">
        SELECT
            id,
            username,
            password,
            role_name AS roleName,
            enabled
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insertAuthUser" parameterType="com.aastudio.domain.auth.AuthVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, role_name, enabled)
        VALUES (#{username}, #{password}, #{roleName}, #{enabled})
    </insert>

    <select id="countUsers" resultType="long">
        SELECT COUNT(*) FROM users
    </select>

    <select id="selectUserList" resultType="com.aastudio.domain.user.UserVO">
        SELECT id, username, password, role_name AS roleName, enabled
        FROM users
        ORDER BY id DESC
    </select>

    <select id="searchUserList" parameterType="string" resultType="com.aastudio.domain.user.UserVO">
        SELECT id, username, password, role_name AS roleName, enabled
        FROM users
        WHERE LOWER(username) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(role_name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(enabled) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
        ORDER BY id DESC
    </select>

    <select id="selectUserById" parameterType="long" resultType="com.aastudio.domain.user.UserVO">
        SELECT id, username, password, role_name AS roleName, enabled
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insertUser" parameterType="com.aastudio.domain.user.UserVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password, role_name, enabled)
        VALUES (#{username}, #{password}, #{roleName}, #{enabled})
    </insert>

    <update id="updateUser" parameterType="com.aastudio.domain.user.UserVO">
        UPDATE users
        SET username = #{username},
            password = #{password},
            role_name = #{roleName},
            enabled = #{enabled}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM users WHERE id = #{id}
    </delete>
</mapper>
