<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aastudio.mapper.ProjectMapper">
    <select id="countProjects" resultType="long">
        SELECT COUNT(*) FROM project
    </select>

    <select id="countProjectsByStatus" parameterType="string" resultType="long">
        SELECT COUNT(*)
        FROM project
        WHERE LOWER(status) = LOWER(#{status})
    </select>

    <select id="selectProjectList" resultType="com.aastudio.domain.project.ProjectVO">
        SELECT
            id,
            name,
            description,
            root_path,
            current_stage,
            status,
            created_at,
            updated_at
        FROM project
        ORDER BY updated_at DESC, id DESC
    </select>

    <select id="searchProjectList" parameterType="string" resultType="com.aastudio.domain.project.ProjectVO">
        SELECT
            id,
            name,
            description,
            root_path,
            current_stage,
            status,
            created_at,
            updated_at
        FROM project
        WHERE LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(description) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
           OR LOWER(status) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
        ORDER BY id DESC
    </select>

    <select id="searchProjectListByStatus" parameterType="string" resultType="com.aastudio.domain.project.ProjectVO">
        SELECT
            id,
            name,
            description,
            root_path,
            current_stage,
            status,
            created_at,
            updated_at
        FROM project
        WHERE LOWER(status) = LOWER(#{status})
        ORDER BY id DESC
    </select>

    <select id="searchProjectListWithStatus" parameterType="map" resultType="com.aastudio.domain.project.ProjectVO">
        SELECT
            id,
            name,
            description,
            root_path,
            current_stage,
            status,
            created_at,
            updated_at
        FROM project
        WHERE (
            LOWER(name) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
            OR LOWER(description) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
            OR LOWER(status) LIKE LOWER(CONCAT('%', #{keyword}, '%'))
        )
        AND LOWER(status) = LOWER(#{status})
        ORDER BY id DESC
    </select>

    <select id="selectProjectById" parameterType="long" resultType="com.aastudio.domain.project.ProjectVO">
        SELECT
            id,
            name,
            description,
            root_path,
            current_stage,
            status,
            created_at,
            updated_at
        FROM project
        WHERE id = #{id}
    </select>

    <insert id="insertProject" parameterType="com.aastudio.domain.project.ProjectVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project (name, description, root_path, current_stage, status, created_at, updated_at)
        VALUES (#{name}, #{description}, #{rootPath}, #{currentStage}, #{status}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="updateProject" parameterType="com.aastudio.domain.project.ProjectVO">
        UPDATE project
        SET name = #{name},
            description = #{description},
            root_path = #{rootPath},
            current_stage = #{currentStage},
            status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteProject" parameterType="long">
        DELETE FROM project WHERE id = #{id}
    </delete>
    <update id="updateCurrentStage">
        UPDATE project SET current_stage=#{currentStage},updated_at=CURRENT_TIMESTAMP WHERE id=#{id}
    </update>
</mapper>
